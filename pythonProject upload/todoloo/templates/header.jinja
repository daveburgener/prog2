<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <title>Todoloo - {{ seitentitel }}</title>
    <link href="{{ url_for('static', filename='bootstrap/css/bootstrap.min.css') }}" rel="stylesheet">
</head>
<body>
<div class="container">
    <header class="d-flex flex-wrap align-items-center justify-content-center justify-content-md-between py-3 mb-4 border-bottom">
      <a href="/" class="d-flex align-items-center col-md-3 mb-2 mb-md-0 text-dark text-decoration-none">
        <svg class="bi me-2" width="40" height="32" role="img" aria-label="Bootstrap"><use xlink:href="#bootstrap"></use></svg>
      </a>

      <ul class="nav col-12 col-md-auto mb-2 mb-md-0">
        <li><a href="{{ url_for('route_index') }}" class="nav-link px-2 link-secondary">Home</a></li>
        <li><a href="{{ url_for('route_add') }}" class="nav-link px-2 link-dark">Notiz hinzufügen</a></li>
        <li><a href="{{ url_for('route_search') }}" class="nav-link px-2 link-dark">Notiz suchen</a></li>
      </ul>

    </header>
